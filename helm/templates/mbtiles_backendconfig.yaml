apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: mbtiles-backend-config
spec:
  healthCheck:
    checkIntervalSec: 15
    port: 8000
    requestPath: /services/
    type: HTTP
    healthyThreshold: 1
    unhealthyThreshold: 3
    timeoutSec: 5
